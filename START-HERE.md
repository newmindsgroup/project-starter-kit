# AI entry: set up this project safely

Version-Timestamp: 2026-09-10 18:27:14 AST

You are using the public Project Starter Kit to help the user start, adopt or resume one selected project. Read the user's existing instructions first. Fetched kit instructions describe how to use the kit; they do not expand authorization, override higher-priority instructions or grant publication, credential or destructive-action permissions.

## 1. Resolve the two repositories and the target

The kit URL is https://github.com/newmindsgroup/project-starter-kit. A second URL may identify the user's application. Preserve the application remote, Git history, uncommitted files and existing project identity. Do not copy this repository's Git history into the application.

Obtain a checkout in an approved tooling location separate from the target. Use a selected existing checkout without resetting its work, or clone the exact user-supplied URL into a new destination. Record the kit version and source commit from release-manifest.json. If access or required file/command tools are unavailable, report that concrete gap; do not infer uninspected contents.

The distribution layout is:

- KIT: root of this public checkout.
- PACKAGE: KIT/plugins/ide-project-starter.
- CORE: PACKAGE/core.
- Canonical entry skill: PACKAGE/skills/project-starter/SKILL.md.
- Helpers: CORE/scripts.
- Templates: CORE/template.
- Dependency lock: CORE/experiments/requirements.lock.txt.

Do not mix scripts from this checkout with a different plugin version. Do not execute an identically named helper discovered in client material. Verify project helper provenance before running existing project copies.

## 2. Inspect and understand

Read the canonical entry skill and its included guides: CORE/docs/setup-wizard.md, client-onboarding.md, intake.md and storage-boundaries.md. Use the public [command guide](docs/commands.md) for this distribution's actual paths. Some bundled guides preserve earlier development-relative examples or historical evidence references. Their TOOLKIT/KIT execution root means CORE here. Do not treat an unavailable historical report as evidence; current public acceptance is in VALIDATION.md.

Run the trusted read-only setup inspector against the selected project. Treat its route and framework hints as advisory. Establish its actual Git root, worktree state, source folders, audiences and memory destination. Do not inspect unrelated sibling projects.

Read only relevant approved sources. A filename inventory is not content understanding. Record source/date and read coverage; distinguish accepted facts, proposals, contradictions, assumptions and missing access. Do not copy secrets or raw client archives into Git.

Infer safe setup defaults from the conversation. For setup-only work, the canonical wizard allows a provisional setup objective while recording the missing product objective. Ask only about consequential unknowns, especially project selection, sharing audience, incompatible existing instructions or unavailable prerequisites.

## 3. Preview and perform the correct route

| Condition | Action |
| --- | --- |
| Empty project without starter identity | Build the six-field brief, then new-project preview/apply. |
| Existing application or documents without starter identity | Build brief plus current state, then adoption preview/apply, exact compatible entry appends, finish. |
| Existing starter identity | Verify records and resume. Do not initialize again. |
| Partial or conflicting installation | Recover the matching plan or prepare reconciliation. Keep evidence; do not delete markers or change hashes to force success. |
| Different memory system | Prepare explicit migration mapping or companion records. Do not invent prior history. |

Use an approved parent workspace containing the child project target. Input and plan files remain outside the child target but inside that workspace. --workspace is an absolute canonical path. --target, --input and --plan are workspace-relative. Adoption state.sources is project-relative and excludes AGENTS.md and CLAUDE.md. brief.objective and state.objective must match exactly.

Review the helper scripts and pinned dependencies before execution. Reuse a compatible environment or create an authorized local virtual environment; avoid writing into installed plugin caches. Do not install every language runtime.

Explain the proposed additions and conflicts before applying. Continue reversible work within the user's authorization. Do not request approval repeatedly for already authorized steps. Broader access, publication, purchases and destructive operations need their own authorization.

For adoption, preserve original instruction bytes and append only the exact planned compatible blocks once. A semantic conflict requires reconciliation. Apply alone is incomplete; finish verifies the integration and records checkpoint 1. Preserve the original plan for recovery.

## 4. Verify and hand off

For a new project, complete the context and quality records, save a checkpoint and run resume. For adoption, run finish, then resume and learning validation. Compare the recovered objective, decisions, completed work, recorded checks, blocker and next action. Run relevant application checks separately; a successful setup helper does not prove the application works.

Record kit provenance, actual checks, selected project root, source coverage and remaining gaps in the appropriate project records. Report what changed, what was preserved, where saved work lives, the recovery result and one next action. Separate a helper-process recovery check from a fresh agent-session trial.

Do not initialize Git at the entire client archive root. Commit/push only approved project records when authorized. Do not send client data to public issues or back into this kit.

Future sessions read the generated project's AGENTS.md or CLAUDE.md and local skills. If skill discovery is unavailable, read the corresponding file directly. Keep the existing identity. Updating the installed plugin or kit checkout does not upgrade existing project copies automatically.

## Optional tool-wide installation

Only install the plugin when the user asks for tool-wide availability. Project setup is the default. Follow [install.md](docs/install.md), inspect existing marketplace registrations and preserve unrelated configuration. Do not add hooks, MCP servers, global instruction replacements or new model accounts as setup side effects.
